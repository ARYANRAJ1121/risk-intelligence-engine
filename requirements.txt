# ============================================================
# Risk Intelligence Engine — Python Dependencies
# ============================================================
# Layer 1: Data Engineering
# Layer 2: ML Modeling (added later)
# Layer 3: Deep Learning (added later)
# Layer 4: Stress Testing (added later)
# Layer 5: Dashboard + Reporting (added later)
# ============================================================

# --- Core Data Engineering (Layer 1) --- #
pyspark>=3.4.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# --- FRED API Integration --- #
fredapi>=0.5.0

# --- Database (optional — Parquet fallback works without these) --- #
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# --- ML Modeling (Layer 2 — will be activated) --- #
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
imbalanced-learn>=0.11.0
shap>=0.43.0
joblib>=1.3.0

# --- Deep Learning (Layer 3 — will be activated) --- #
tensorflow>=2.14.0

# --- Visualization --- #
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Dashboard (Layer 5 — will be activated) --- #
streamlit>=1.28.0

# --- PDF Reports (Layer 5 — will be activated) --- #
reportlab>=4.0.0

# --- Testing --- #
pytest>=7.4.0

# --- Utilities --- #
tqdm>=4.65.0
